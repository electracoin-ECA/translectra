extends ../layout.pug

block meta
  meta(name='model' content=JSON.stringify('version'))
  meta(name='schema' content=JSON.stringify([
    { name: 'project', type: 'foreign', isColumn: true, isField: true, label: 'Project' },
    { name: 'name', type: 'text', isColumn: true, isField: true, label: 'Name' },
    { name: 'updatedAt', type: 'date', isColumn: true, isField: false, label: 'Updated' },
    { name: 'createdAt', type: 'date', isColumn: true, isField: false, label: 'Created' },
  ]))
  meta(name='meta' content=JSON.stringify({
    defaultName: 'name',
    foreignData: {
      project: projects,
    },
    sortBy: 'updatedAt',
    sortOrder: false,
  }))

block content
  h1(class='display-1 no-select') Versions
  div(id='data-table') Loading...
